# vault-notes Code Index

Use this file to locate functions before reading source. Then use `Read` with `offset`/`limit` to jump directly to the relevant lines.

**Keep this file updated.** Any time a function is added, renamed, moved, or removed — update the entry here in the same task as the code change.

---

<!-- Populated as implementation progresses. See CLAUDE.md Architecture Summary for file responsibilities. -->
